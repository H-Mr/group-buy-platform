```text
com.example.ecommerce
├── application          // 应用层
│   ├── dto              // 输入输出DTO
│   │   ├── request      // 请求DTO（如CreateOrderRequest）
│   │   └── response     // 响应DTO（如OrderResponse）
│   ├── service          // 应用服务（如OrderApplicationService）
│   └── event            // 应用层事件处理（如PaymentCompletedEventHandler）
├── domain               // 领域层
│   ├── model            // 领域模型
│   │   ├── order        // 订单子域
│   │   │   ├── entity   // 实体（如Order、OrderItem）
│   │   │   ├── vo       // 值对象（如Money、Address）
│   │   │   ├── aggregate// 聚合根（如OrderAggregate）
│   │   │   ├── event    // 领域事件（如OrderCreatedEvent）
│   │   │   └── service  // 领域服务（如OrderDomainService）
│   │   └── inventory    // 库存子域（结构同上）
│   └── repository       // 仓储接口（如OrderRepository）
├── infrastructure       // 基础设施层
│   ├── persistence      // 持久化实现
│   │   ├── repository   // 仓储实现（如OrderRepositoryImpl）
│   │   ├── entity       // 数据库实体（如OrderJpaEntity）
│   │   └── converter    // 对象转换器（如OrderConverter）
│   ├── event            // 领域事件发布实现（如DomainEventPublisher）
│   ├── client           // 外部服务客户端（如支付接口客户端）
│   └── util             // 工具类
├── presentation         // 表现层
│   ├── controller       // 控制器（如OrderController）
│   ├── exception        // 全局异常处理
│   └── validator        // 请求参数校验
└── EcommerceApplication.java // 应用入口
```